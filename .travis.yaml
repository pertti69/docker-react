language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t pertti69/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true pertti69/docker-react npm run test